{
  "devDependencies": {
    "stylelint": "^16.25.0",
    "stylelint-config-standard": "^39.0.1",
    "stylelint-order": "^7.0.0",
    "wrangler": "^4.115.0"
  },
  "scripts": {
    "lint:css": "stylelint \"styles/**/*.css\"",
    "build:info": "node scripts/generate-build-info.mjs",
    "predeploy:api": "npm run build:info",
    "deploy:api": "wrangler deploy -c cloudflare/wrangler.api.toml"
  }
}
