/*
 * Legacy parallax hook.
 * Several public pages still include this file; keep it as a no-op until a
 * scoped parallax implementation is restored or the script tags are removed.
 */
(function () {
  'use strict';
})();
